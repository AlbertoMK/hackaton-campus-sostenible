import axios from 'axios';
/*
import type { DirectorDTO } from './interfaces';

const api = axios.create({ baseURL: 'http://localhost:8080/api' });

export const fetchFilms = async(): Promise<FilmDTO[]> => {
    const response = await api.get<FilmDTO[]>('/films');
    return response.data;
};
*/